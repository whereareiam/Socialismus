name: ${name}
version: '${version}'
main: me.whereareiam.socialismus.SocialismusPaper
api-version: '1.20'
dependencies:
  bootstrap:
    PlaceholderAPI:
      load: AFTER
      required: false
      join-classpath: false
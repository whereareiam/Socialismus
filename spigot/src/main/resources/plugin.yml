name: ${project.parent.artifactId}
version: '${project.parent.version}'
main: me.whereareiam.socialismus.spigot.SocialismusSpigot
api-version: '1.19'
softdepend: [
  PlaceholderAPI,
  ProtocolLib
]

libraries:
  - com.google.inject:guice:${guice.version}
  - net.elytrium:serializer:${serializer.version}

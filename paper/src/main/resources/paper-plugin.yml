name: ${project.artifactId}
version: '${project.version}'
main: me.whereareiam.socialismus.SocialismusPaper
api-version: '1.19'
dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: true
    ProtocolLib:
      load: BEFORE
      required: false
      join-classpath: true